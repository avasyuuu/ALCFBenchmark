# Shell scripts and Python must keep LF endings. A CRLF shebang line fails on
# Linux with "bad interpreter: /bin/bash^M", which is a confusing way to lose
# an hour on Aurora.
*.sh text eol=lf
*.py text eol=lf
*.json text eol=lf
*.md text eol=lf
