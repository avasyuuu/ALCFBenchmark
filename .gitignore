__pycache__/
*.py[cod]
.venv/
venv/

# Datasets — re-download with `python -m benchmark.prepare`
data/

# PBS job output
*.o[0-9]*
*.e[0-9]*

# Keep results/ tracked (they are the deliverable) but not scratch files
results/*.tmp
*.csv
